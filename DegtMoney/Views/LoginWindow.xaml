<Window x:Class="DegtMoney.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DegtMoney.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="350" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Логин" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
        <TextBlock Grid.Row="2" Text="Пароль" FontWeight="Bold" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="3" x:Name="PasswordBox" Margin="0,0,0,20"/>
        <Button Grid.Row="4" Content="Войти" Command="{Binding LoginCommand}" Height="30" Margin="0,0,0,10"/>
        <Button Grid.Row="5" Content="Регистрация" Command="{Binding OpenRegisterCommand}" Height="30"/>
    </Grid>
</Window>
