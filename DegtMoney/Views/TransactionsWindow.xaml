<Window x:Class="DegtMoney.Views.TransactionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DegtMoney.Views"
        mc:Ignorable="d"
        Title="TransactionsWindow" Height="500" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <DatePicker SelectedDate="{Binding FilterStart}" Width="120"/>
            <DatePicker SelectedDate="{Binding FilterEnd}" Width="120" Margin="10,0"/>
            <ComboBox ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCategory}" Width="150" Margin="0,0,10,0"/>
            <Button Content="Фильтр" Command="{Binding ApplyFilterCommand}" Margin="0,0,10,0"/>
            <Button Content="Добавить" Command="{Binding AddTransactionCommand}"/>
        </StackPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredTransactions}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=d}"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:F2}}"/>
                <DataGridTextColumn Header="Примечание" Binding="{Binding NoteEncrypted}"/>
                <DataGridTemplateColumn Header="Действия">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Изменить" Command="{Binding DataContext.EditTransactionCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                <Button Content="Удалить" Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="5,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
