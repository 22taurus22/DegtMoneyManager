<Window x:Class="DegtMoney.Views.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DegtMoney.Views"
        mc:Ignorable="d"
        Title="ReportWindow" Height="250" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Начало периода:" FontWeight="Bold"/>
        <DatePicker Grid.Row="1" SelectedDate="{Binding StartDate}" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="2" Text="Конец периода:" FontWeight="Bold"/>
        <DatePicker Grid.Row="3" SelectedDate="{Binding EndDate}" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="CSV" Command="{Binding ExportCsvCommand}" Width="80" Height="30" Margin="5"/>
            <Button Content="PDF" Command="{Binding ExportPdfCommand}" Width="80" Height="30" Margin="5"/>
        </StackPanel>
        <TextBlock Grid.Row="5" Text="{Binding IsLoading, StringFormat='Загрузка...'}" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Window>
