<Window x:Class="DegtMoney.Views.CategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DegtMoney.Views"
        mc:Ignorable="d"
        Title="CategoriesWindow" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="5">
            <TextBlock Text="Доходы" FontSize="18" FontWeight="Bold" TextAlignment="Center"/>
            <ListBox ItemsSource="{Binding IncomeCategories}" DisplayMemberPath="Name" Height="300"/>
            <Button Content="Добавить категорию дохода" Command="{Binding AddIncomeCategoryCommand}" Margin="0,10,0,5"/>
        </StackPanel>
        <StackPanel Grid.Column="1" Margin="5">
            <TextBlock Text="Расходы" FontSize="18" FontWeight="Bold" TextAlignment="Center"/>
            <ListBox ItemsSource="{Binding ExpenseCategories}" DisplayMemberPath="Name" Height="300"/>
            <Button Content="Добавить категорию расхода" Command="{Binding AddExpenseCategoryCommand}" Margin="0,10,0,5"/>
        </StackPanel>
    </Grid>
</Window>
