<Window x:Class="DegtMoney.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DegtMoney"
        mc:Ignorable="d"
        Title="MainWindow" WindowState="Maximized" WindowStyle="SingleBorderWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <DatePicker SelectedDate="{Binding StartDate}" />
            <DatePicker SelectedDate="{Binding EndDate}" Margin="10,0,10,0"/>
            <Button Content="Обновить" Command="{Binding RefreshCommand}" Margin="0,0,10,0"/>
            <Button Content="Транзакции" Command="{Binding OpenTransactionsCommand}" Margin="0,0,10,0"/>
            <Button Content="Категории" Command="{Binding OpenCategoriesCommand}" Margin="0,0,10,0"/>
            <Button Content="Цели" Command="{Binding OpenSavingGoalsCommand}" Margin="0,0,10,0"/>
            <Button Content="Отчёты" Command="{Binding OpenReportsCommand}" Margin="0,0,10,0"/>
            <Button Content="Настройки" Command="{Binding OpenSettingsCommand}" Margin="0,0,10,0"/>
            <Button Content="Экспорт CSV" Command="{Binding ExportCsvCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="10">
                <TextBlock Text="Доходы" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalIncome, StringFormat={}{0:F2} {1}}" FontSize="28" Foreground="Green"/>
                <TextBlock Text="Расходы" FontSize="20" FontWeight="Bold" Margin="0,20,0,0"/>
                <TextBlock Text="{Binding TotalExpense, StringFormat={}{0:F2} {1}}" FontSize="28" Foreground="Red"/>
            </StackPanel>
            <lvc:PieChart Grid.Column="1" Series="{Binding PieChartSeries}" LegendLocation="Right"/>
        </Grid>
        <TextBlock Grid.Row="2" Text="{Binding CurrentCurrency, StringFormat='Валюта: {0}'}" HorizontalAlignment="Right" Margin="5"/>
    </Grid>
</Window>
